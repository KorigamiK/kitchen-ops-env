# Copy this to .env locally if you want to run inference without exporting vars manually.
# Do not commit the real .env file.

API_BASE_URL=https://models.github.ai/inference
MODEL_NAME=openai/gpt-4o
HF_TOKEN=replace-with-your-secret
KITCHEN_ENV_URL=http://localhost:8000

